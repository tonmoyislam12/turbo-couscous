{
  "name": "Compression",
  "description": "hello",
  "repository": "https://github.com/tonmoyislam12/turbo-couscous",
  "stack": "container",
    "env": {
    "STREAM_KEY": {
      "description": "KEY FOR STREAMING",
      "required": false
    },
    "STREAM_URL": {
      "description": "STREAM URL without leading '/'",
      "required": false
    }
   }
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "eco"
    }
  }
}
